services:
  - type: web
    name: wordbomb
    runtime: python
    buildCommand: pip install flask gunicorn gevent
    startCommand: gunicorn app:app --worker-class gevent --workers 1 --bind 0.0.0.0:$PORT --timeout 120 --keep-alive 5
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
